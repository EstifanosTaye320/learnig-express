// fill in the following

let byid; // get element li by id
let byclass; // get element li by class name
let byname; // get element input by name
let bytagname; // get div by tagname

console.log(byid);
if(byclass) {
    console.log(byclass[0]);
}
if(byname) {
    console.log(byname[0]);
}
if(bytagname) {
    console.log(bytagname[0]);
}
// simulation of fetch() function in javascript

function fetchdata(url, callback) {
  const useurl = url;
  setTimeout(/**code here*/);
}

fetchdata("http://localhost:3000/user/1", (data) => {
  console.log(data);
});

// modify the code above so that it waits 3 seconds before executing a function that does the following
// 1. declare and assign a variable data which is an object that has id, name, pet and otherpet
// 2. calls callback(data)

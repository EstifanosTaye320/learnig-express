// fill in the following

let byid; // code here: get element li by id
let byclass; // code here: get element li by class name
let byname; // code here: get element input by name
let bytagname; // code here: get div by tagname

console.log(byid);
if (byclass) {
  console.log(byclass[0]);
}
if (byname) {
  console.log(byname[0]);
}
if (bytagname) {
  console.log(bytagname[0]);
}

// Exercise
// compare the getElement methods with querySelector?
// find out what HTMLCollections, and NodeList have in common
// find out what makes them different?
// which of the methods return a NodeList?

// near everything is related to the window global object

console.log(window);

// some important objects in window
console.log(window.location);
console.log(window.console);
console.log(window.document);

// global declaration
var x = "estif is part of the window object";

console.log("x" in window);

// Exercise
// Explore the uses of the location object
// Explore the uses of the console object
// Explore the uses of the document object
